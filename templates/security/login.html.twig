{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block content %}
<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            Vous êtes connecté en tant que {{ app.user.username }}, <a href="{{ path('app_logout') }}">Déconnectez-vous.</a>
        </div>
    {% endif %}

    <h1 class="border-bottom h3 my-5 pb-5 font-weight-normal text-center" id="accroche">Connexion client</h1>
    <div class="rounded border border-dark bg-light pt-5 pb-4 col-10 col-md-6 mx-auto" id="shadow">
        <div class="form-group row mx-auto col-10 col-md-7">
            <label for="inputUsername" class="col-sm-2 col-form-label"></label>
            <div class="col-sm-10">
                <p class="text-center">Vous n'êtes pas encore client: <a href="register">Cliquez ici</a></p>
                <input type="text" value="{{ last_username }}" name="username" id="inputUsername" 
                class="form-control" placeholder="Nom d'utilisateur">
            </div>
        </div>
        <div class="form-group row mx-auto col-10 col-md-7">
            <label for="inputPassword" class="col-sm-2 col-form-label"></label>
            <div class="col-sm-10 mb-5">
                <input type="password" name="password" id="inputPassword" class="form-control" 
                autocomplete="current-password" placeholder="Votre mot de passe" required>
                <p class="text-right mt-2"><a href="#">Mot de passe oublié ?</a></p>
            </div>
        </div>
        <div class="text-center">
            <button class="btn btn-lg btn-warning mt-2 ml-5" type="submit">
                Connexion
            </button>
        </div>
        
        <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}"
        >
    </div>
    <br/><br/>
    
    {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
    #}
</form>
{% endblock %}